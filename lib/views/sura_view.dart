import 'package:flutter/material.dart';

class SuraView extends StatelessWidget {
  const SuraView({super.key});
  static const String id = "sura-view";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("HELLLO MAN"),
      ),
    );
  }
}
